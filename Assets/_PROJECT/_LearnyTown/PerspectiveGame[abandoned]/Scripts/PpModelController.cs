using UnityEngine;

namespace LearnyTown.PerspectiveGame
{
    public class PpModelController : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
